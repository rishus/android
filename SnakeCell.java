package com.groupproject.snake;

/**
 * This class is for setting up the switch for maintaining the current mode
 * of the game (remembering what button was clicked most recently). The
 * switch can take 4 states: setStartMode, setGoalMode, drawMode, eraseMode.
 *
 * @author Pat Butler (patsfan1)
 * @author Brannon Mason (brannon1)
 * @author Rishu Saxena (rishus)
 * @version 2013.11.08
 */
public enum SnakeCell
{
    // SnakeCell types
        WALL,
        FIELD,
        SNAKE,
        FRUIT

};
